# Core backend configuration
MONGO_URI=mongodb://127.0.0.1:27017/jobdekho
JWT_SECRET=replace-with-a-long-random-secret
JWT_LIFETIME=7d
CLIENT_URL=http://localhost:3000

# Gmail (use a Google App Password, not the normal account password)
MAIL_SERVICE=gmail
MAIL_USER=your-account@gmail.com
MAIL_PASS=your-16-character-app-password
MAIL_FROM="JobDekho <your-account@gmail.com>"

# For another SMTP provider, remove MAIL_SERVICE and set these instead:
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-smtp-user
# SMTP_PASS=your-smtp-password
